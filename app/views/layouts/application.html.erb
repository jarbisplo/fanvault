<!DOCTYPE html>
<html lang="en" class="h-full bg-black">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Mauricio Dubon</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "app", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>
<body class="h-full font-sans antialiased text-white bg-black">

  <!-- Navbar -->
  <nav class="bg-zinc-900 border-b border-zinc-600/40/50 sticky top-0 z-50">
    <div class="max-w-6xl mx-auto px-5 py-3.5 flex items-center justify-between">
      <%= link_to root_path, class: "text-lg font-extrabold tracking-tight text-white" do %>
        Mauricio<span class="text-red-600">Dubon</span>
      <% end %>

      <div class="flex items-center gap-3 text-sm font-medium">
        <% if user_signed_in? %>
          <% if current_user.admin? %>
            <%= link_to "Dashboard", admin_root_path, class: "text-zinc-400 hover:text-white" %>
          <% else %>
            <%= link_to "Videos", videos_path, class: "text-zinc-400 hover:text-white" %>
          <% end %>
          <%= link_to "Sign out", destroy_user_session_path, data: { turbo_method: :delete },
              class: "text-zinc-600 hover:text-zinc-300 text-xs" %>
        <% else %>
          <%= link_to "Sign in", new_user_session_path, class: "text-zinc-400 hover:text-white" %>
          <%= link_to "Get Access", pricing_path,
              class: "bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded-full transition font-semibold text-sm" %>
        <% end %>
      </div>
    </div>
  </nav>

  <!-- Flash -->
  <% if notice.present? %>
    <div class="max-w-6xl mx-auto px-5 mt-4">
      <div class="bg-zinc-800 border border-zinc-600/40 text-zinc-200 px-4 py-3 rounded-xl text-sm">
        <%= notice %>
      </div>
    </div>
  <% end %>
  <% if alert.present? %>
    <div class="max-w-6xl mx-auto px-5 mt-4">
      <div class="bg-red-950 border border-red-800 text-red-300 px-4 py-3 rounded-xl text-sm">
        <%= alert %>
      </div>
    </div>
  <% end %>

  <%= yield %>

</body>
</html>
