<!DOCTYPE html>
<html lang="en" class="h-full bg-white">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>FanVault</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "app", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>
<body class="h-full font-sans antialiased text-gray-900">

  <!-- Navbar -->
  <nav class="bg-white border-b border-gray-100 sticky top-0 z-50">
    <div class="max-w-6xl mx-auto px-5 py-4 flex items-center justify-between">
      <%= link_to root_path, class: "text-xl font-bold tracking-tight text-gray-900" do %>
        Fan<span class="text-green-600">Vault</span>
      <% end %>

      <div class="flex items-center gap-4 text-sm font-medium">
        <% if user_signed_in? %>
          <% if current_user.admin? %>
            <%= link_to "Dashboard", admin_root_path, class: "text-gray-600 hover:text-gray-900" %>
          <% else %>
            <%= link_to "Videos", videos_path, class: "text-gray-600 hover:text-gray-900" %>
          <% end %>
          <%= link_to "Sign out", destroy_user_session_path, data: { turbo_method: :delete },
              class: "text-gray-500 hover:text-gray-700" %>
        <% else %>
          <%= link_to "Sign in", new_user_session_path, class: "text-gray-600 hover:text-gray-900" %>
          <%= link_to "Get Access", pricing_path,
              class: "bg-green-600 hover:bg-green-700 text-white px-5 py-2.5 rounded-full transition font-semibold" %>
        <% end %>
      </div>
    </div>
  </nav>

  <!-- Flash -->
  <% if notice.present? %>
    <div class="max-w-6xl mx-auto px-5 mt-4">
      <div class="bg-green-50 border border-green-200 text-green-800 px-4 py-3 rounded-xl text-sm">
        <%= notice %>
      </div>
    </div>
  <% end %>
  <% if alert.present? %>
    <div class="max-w-6xl mx-auto px-5 mt-4">
      <div class="bg-red-50 border border-red-200 text-red-800 px-4 py-3 rounded-xl text-sm">
        <%= alert %>
      </div>
    </div>
  <% end %>

  <%= yield %>

</body>
</html>
