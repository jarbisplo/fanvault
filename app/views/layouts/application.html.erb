<!DOCTYPE html>
<html lang="en" class="h-full bg-black">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Train Like Dubi — Mauricio Dubón's Training Platform</title>
  <meta name="description" content="Exclusive training content from Mauricio Dubón — 2x Gold Glove winner, World Series champion. Learn the drills that built an MLB career. Members only.">

  <!-- Open Graph -->
  <meta property="og:type"        content="website">
  <meta property="og:url"         content="https://trainlikedubi.com/">
  <meta property="og:title"       content="Train Like Dubi — Mauricio Dubón's Training Platform">
  <meta property="og:description" content="Exclusive training content from Mauricio Dubón — 2x Gold Glove winner, World Series champion. Learn the drills that built an MLB career.">
  <meta property="og:image"       content="https://trainlikedubi.com/og-image.jpg">

  <!-- Twitter Card -->
  <meta name="twitter:card"        content="summary_large_image">
  <meta name="twitter:title"       content="Train Like Dubi — Mauricio Dubón's Training Platform">
  <meta name="twitter:description" content="Exclusive training content from Mauricio Dubón — 2x Gold Glove winner, World Series champion.">
  <meta name="twitter:image"       content="https://trainlikedubi.com/og-image.jpg">

  <!-- Favicon -->
  <link rel="icon" type="image/x-icon" href="/favicon.ico">
  <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32.png">
  <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "app", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>
<body class="h-full font-sans antialiased text-white bg-black">

  <!-- Navbar — hidden on mobile for logged-in subscribers (they use bottom nav) -->
  <nav class="sticky top-0 z-50 <%= user_signed_in? && !current_user.admin? ? 'hidden md:block' : '' %>" style="background: rgba(0,0,0,0.65); backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px); border-bottom: 1px solid rgba(255,255,255,0.06);">
    <div class="max-w-6xl mx-auto px-5 py-3.5 flex items-center justify-between">
      <%= link_to root_path, class: "text-lg font-extrabold tracking-tight text-white" do %>
        Mauricio<span class="text-red-500">Dubon</span>
      <% end %>

      <div class="flex items-center gap-3 text-sm font-medium">
        <% if user_signed_in? %>
          <% if current_user.admin? %>
            <%= link_to "Dashboard", admin_root_path, class: "text-white/60 hover:text-white transition" %>
          <% else %>
            <%= link_to "Videos", videos_path, class: "text-white/60 hover:text-white transition" %>
          <% end %>
          <%= button_to "Sign out", destroy_user_session_path, method: :delete,
              class: "text-white/30 hover:text-white/60 text-xs transition bg-transparent border-0 cursor-pointer p-0" %>
        <% else %>
          <%= link_to "Sign in", new_user_session_path, class: "text-white/60 hover:text-white transition" %>
          <%= link_to "Get Access", new_user_registration_path,
              class: "px-4 py-2 rounded-full font-bold text-sm text-white",
              style: "background: rgba(220,38,38,0.85); backdrop-filter: blur(8px); -webkit-backdrop-filter: blur(8px); border: 1px solid rgba(255,255,255,0.12); box-shadow: 0 4px 16px rgba(220,38,38,0.25);" %>
        <% end %>
      </div>
    </div>
  </nav>

  <!-- Flash -->
  <% if notice.present? %>
    <div class="max-w-6xl mx-auto px-5 mt-4">
      <div class="px-4 py-3 rounded-2xl text-sm text-white" style="background: rgba(255,255,255,0.06); border: 1px solid rgba(255,255,255,0.1); backdrop-filter: blur(8px);">
        <%= notice %>
      </div>
    </div>
  <% end %>
  <% if alert.present? %>
    <div class="max-w-6xl mx-auto px-5 mt-4">
      <div class="px-4 py-3 rounded-2xl text-sm text-red-300" style="background: rgba(220,38,38,0.1); border: 1px solid rgba(220,38,38,0.2); backdrop-filter: blur(8px);">
        <%= alert %>
      </div>
    </div>
  <% end %>

  <%= yield %>

  <!-- Bottom nav — logged-in members on mobile only -->
  <% if user_signed_in? && !current_user.admin? %>
    <nav class="md:hidden fixed bottom-0 inset-x-0 border-t border-white/5 flex items-center justify-around px-2 py-2 z-50"
         style="background: rgba(10,10,10,0.97); backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px);">
      <%= link_to videos_path,
          class: "flex flex-col items-center gap-1 px-5 py-1.5 rounded-xl #{controller_name == 'videos' && action_name == 'index' ? 'text-red-400' : 'text-zinc-500'}" do %>
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"/>
        </svg>
        <span class="text-[10px] font-semibold">Home</span>
      <% end %>
      <%= link_to videos_path,
          class: "flex flex-col items-center gap-1 px-5 py-1.5 rounded-xl #{controller_name == 'videos' && action_name == 'show' ? 'text-red-400' : 'text-zinc-500'}" do %>
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
        </svg>
        <span class="text-[10px] font-semibold">Browse</span>
      <% end %>
      <%= button_to destroy_user_session_path, method: :delete,
          class: "flex flex-col items-center gap-1 px-5 py-1.5 rounded-xl text-zinc-500 bg-transparent border-0 cursor-pointer" do %>
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"/>
        </svg>
        <span class="text-[10px] font-semibold">Sign out</span>
      <% end %>
    </nav>
  <% end %>

</body>
</html>
