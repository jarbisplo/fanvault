<!DOCTYPE html>
<html lang="en" class="h-full bg-gray-50">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Admin — FanVault</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "app", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>
<body class="h-full flex font-sans antialiased text-gray-900">

  <!-- Sidebar -->
  <aside class="w-60 bg-white border-r border-gray-100 flex flex-col shrink-0 shadow-sm">
    <div class="px-6 py-5 border-b border-gray-100">
      <p class="text-lg font-bold text-gray-900">Fan<span class="text-green-600">Vault</span></p>
      <p class="text-xs text-gray-400 mt-0.5">Admin Panel</p>
    </div>

    <nav class="flex-1 px-3 py-4 space-y-1 text-sm font-medium">
      <%= link_to admin_root_path,
          class: "flex items-center gap-3 px-3 py-2.5 rounded-xl #{controller_name == 'dashboard' ? 'bg-green-50 text-green-700' : 'text-gray-600 hover:bg-gray-50 hover:text-gray-900'}" do %>
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2V6zm10 0a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2V6zM4 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2v-2zm10 0a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2v-2z"/></svg>
        Dashboard
      <% end %>

      <%= link_to admin_videos_path,
          class: "flex items-center gap-3 px-3 py-2.5 rounded-xl #{controller_name == 'videos' ? 'bg-green-50 text-green-700' : 'text-gray-600 hover:bg-gray-50 hover:text-gray-900'}" do %>
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 10l4.553-2.069A1 1 0 0121 8.87v6.26a1 1 0 01-1.447.894L15 14M3 8a2 2 0 012-2h8a2 2 0 012 2v8a2 2 0 01-2 2H5a2 2 0 01-2-2V8z"/></svg>
        Videos
      <% end %>

      <%= link_to admin_subscribers_path,
          class: "flex items-center gap-3 px-3 py-2.5 rounded-xl #{controller_name == 'subscribers' ? 'bg-green-50 text-green-700' : 'text-gray-600 hover:bg-gray-50 hover:text-gray-900'}" do %>
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0z"/></svg>
        Subscribers
      <% end %>
    </nav>

    <div class="px-3 py-4 border-t border-gray-100">
      <%= link_to destroy_user_session_path, data: { turbo_method: :delete },
          class: "flex items-center gap-3 px-3 py-2.5 rounded-xl text-gray-400 hover:text-gray-600 hover:bg-gray-50 text-sm font-medium" do %>
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"/></svg>
        Sign out
      <% end %>
    </div>
  </aside>

  <!-- Main -->
  <div class="flex-1 flex flex-col overflow-hidden">
    <header class="bg-white border-b border-gray-100 px-6 py-4 flex items-center justify-between shrink-0">
      <h1 class="text-sm font-semibold text-gray-500 uppercase tracking-widest">
        <%= controller_name.titleize %>
      </h1>
      <%= link_to "Upload Video", new_admin_video_path,
          class: "bg-green-600 hover:bg-green-700 text-white text-sm font-semibold px-5 py-2.5 rounded-full transition" %>
    </header>

    <% if notice.present? %>
      <div class="mx-6 mt-4 bg-green-50 border border-green-200 text-green-800 px-4 py-3 rounded-xl text-sm">
        <%= notice %>
      </div>
    <% end %>
    <% if alert.present? %>
      <div class="mx-6 mt-4 bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-xl text-sm">
        <%= alert %>
      </div>
    <% end %>

    <main class="flex-1 overflow-y-auto px-6 py-6">
      <%= yield %>
    </main>
  </div>

</body>
</html>
