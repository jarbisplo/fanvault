<div class="bg-gray-50 min-h-screen py-20">
  <div class="max-w-md mx-auto px-5">
    <div class="text-center mb-10">
      <h1 class="text-4xl font-extrabold text-gray-900 mb-3">One plan. Full access.</h1>
      <p class="text-gray-500">Everything in the library. New content every week. Cancel anytime.</p>
    </div>

    <div class="bg-white rounded-3xl shadow-xl border border-gray-100 overflow-hidden">
      <!-- Top band -->
      <div class="bg-green-600 px-8 py-6 text-white text-center">
        <p class="text-green-200 text-sm font-medium uppercase tracking-widest mb-1">Monthly Plan</p>
        <div class="text-6xl font-extrabold">
          $19
          <span class="text-2xl font-normal text-green-200">/ mo</span>
        </div>
      </div>

      <div class="px-8 py-8">
        <ul class="space-y-3 mb-8 text-sm text-gray-700">
          <% [
            "Full video library — available immediately",
            "New training videos every week",
            "Stream on any device, any time",
            "Cancel anytime, no commitment"
          ].each do |item| %>
            <li class="flex items-center gap-3">
              <span class="w-5 h-5 bg-green-100 rounded-full flex items-center justify-center shrink-0">
                <svg class="w-3 h-3 text-green-600" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/>
                </svg>
              </span>
              <%= item %>
            </li>
          <% end %>
        </ul>

        <% if user_signed_in? %>
          <% if current_user.active_subscriber? %>
            <div class="text-center py-3 bg-green-50 border border-green-200 rounded-xl text-green-700 font-semibold text-sm mb-4">
              You already have an active subscription
            </div>
            <%= link_to "Watch Videos", videos_path,
                class: "block text-center w-full text-green-600 font-semibold text-sm hover:underline" %>
          <% else %>
            <%= button_to "Subscribe with Stripe", subscribe_path,
                class: "block w-full bg-green-600 hover:bg-green-700 text-white font-bold py-4 rounded-2xl transition text-center" %>
          <% end %>
        <% else %>
          <%= link_to "Create account to subscribe", new_user_registration_path,
              class: "block w-full bg-green-600 hover:bg-green-700 text-white font-bold py-4 rounded-2xl transition text-center" %>
          <p class="text-center text-gray-400 text-xs mt-4">
            Already have an account?
            <%= link_to "Sign in", new_user_session_path, class: "text-green-600 hover:underline" %>
          </p>
        <% end %>
      </div>
    </div>
  </div>
</div>
